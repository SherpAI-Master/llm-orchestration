# Detecting formatting values
services:

  codebase:
    build: ./images/detection/formatting/tier1/codebase
    container_name: codebase-detection-formatting
    image: homeserver:5000/llm-orchestration/detection_formatting_tier1_codebase:latest
    volumes:
      - ${INPUT}:/job/input.jsonl
      - ${OUTPUT}:/job/output.jsonl
