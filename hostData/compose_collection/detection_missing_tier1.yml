# Detecting missing values
services:

  codebase:
    build: ./images/detection/missing/tier1/codebase
    container_name: codebase
    image: homeserver:5000/llm-orchestration/detection_missing_tier1_codebase:latest
    volumes:
      - ${INPUT}:/job/input.jsonl
      - ${OUTPUT}:/job/output.jsonl
